name: TEMP fix mobile scroll controls v2

on:
  push:
    branches:
      - feature/app-visual-system
    paths:
      - .github/workflows/temp-fix-mobile-scroll-controls.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/app-visual-system
          fetch-depth: 0
      - name: Apply exact mobile fixes
        run: python scripts/temp_fix_mobile_scroll_controls.py
      - name: Validate build
        run: |
          npm ci
          npm run generate
      - name: Commit patch
        run: |
          git config user.name "cuebooker-workspace-bot"
          git config user.email "actions@users.noreply.github.com"
          git add app/pages/workspace.vue assets/css/workspace-responsive-final.css
          git commit -m "Pin mobile booking scroll and shrink profile controls"
          git push origin HEAD:feature/app-visual-system
